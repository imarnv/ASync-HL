# Leave AGENT_API_URL unset to run against the local stub in lib/agent/core.ts.
# Set it to the backend team's endpoint and every channel switches over.
AGENT_API_URL=
AGENT_API_KEY=
AGENT_TIMEOUT_MS=15000
